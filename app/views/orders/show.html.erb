<h1>Order created</h1>

<h2>Order #4560 <%=@order.id %>Created</h2>
<h2><%= @order.user.name%></h2>

<% @order.carted_products.each do%>|carted_product|
<h2><%= carted_product.product.name %> - <%= carted_product.quantity %> X <%= number_to_currency(carted_product.product.price) %>